// Interfaz TypeScript llamada KeypadButton para representar la estructura de los botones del teclado
export interface KeypadButton {
  icon: string;
  color: string;
  tooltip: string;
  action: string;
}
